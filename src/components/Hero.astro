---
import { marked } from 'marked';
import Button from './Button.astro';
import siteConfig from '../data/site-config';

const hero = {
    //title: 'Hi There & Welcome to My Corner of the Web!',
    text: `I'm **Ali Aboelela** (the sibling on the right). I’m a software engineer. I’m a problem driven person. I let problems I care about guide my long term professional and personal goals.<br> This site has 2 functions <br><br>For more info on my technical skills and interests, please check out the careers tab. There you’ll also find articles detailing and reflecting on some of the important experiences I’ve had in my professional career. Outside of work I love the outdoors. Check out my blog for everything else.`,
    image: {
        src: '/hero.JPG',
        alt: 'A picture of ali with his siblings'
    }
    //actions: [
    //    {
    //        text: 'Get in Touch',
    //        href: '/contact'
    //    }
    //]
};
---

<style>
    .img {
        max-height: 60vh;
        position: relative;
        left: 50%;
        transform: translateX(-50%);
    }
</style>

<section class="w-full flex flex-col gap-8 mb-16 sm:mb-24">
    {hero.title && <h1 class="text-3xl leading-tight font-serif font-medium sm:text-5xl sm:leading-tight">{hero.title}</h1>}

    <!--
    {
        hero.image?.src && (
            <figure>
                <img class="img" src={'/hero.JPG'} loading="lazy" decoding="async" alt={'A picture of ali with his siblings'} />
                {hero.image.caption && <figcaption class="mt-1.5 text-xs sm:text-sm">{hero.image.caption}</figcaption>}
            </figure>
        )
    }
    -->

    <div class="max-w-none prose prose-dante sm:prose-lg">
        <p>
            I'm <b>Ali Aboelela</b>, I’m a software engineer. I’m a problem driven person: I let problems I care about guide my long term professional and
            personal goals. This blog has 2 functions:
            <ul>
                <li>For employers, professors, researchers, colleagues, friends, and any other stakeholders: Introduce them to my interests, and mindset.</li>
                <li>For myself: In addition to being an archive of my career experiences and lessons learned; a creative outlet to yap to the void</li>
            </ul>
            <br />For more info on my technical skills, interests, and career experiences please check out the careers tab. Check out my blog for everything
            else.
        </p>
        <div></div>

        <!-- This snippet goes through the actions section in the site-config. It creates buttons for each action. Buttons are just links. Not currently in use  -->
        {
            hero.actions && hero.actions.length > 0 && (
                <div class="flex flex-wrap gap-4">
                    {hero.actions.map((action) => (
                        <Button href={action.href}>{action.text}</Button>
                    ))}
                </div>
            )
        }
    </div>
</section>
